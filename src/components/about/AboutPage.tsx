import * as React from 'react';

const AboutPage = (): JSX.Element => {
    return (
        <div>
            <h1>About</h1>
            <p>This application uses React, Redux and other libraries</p>
        </div>
    );
};

export default AboutPage;
